#!/usr/bin/python3
"""
To check if it is a subclass
"""


def is_same_class(obj, a_class):
    """
    function to check if an obj is a_class inst
    """
    if (isinstance(obj, a_class)):
        return True
    else:
        return False
